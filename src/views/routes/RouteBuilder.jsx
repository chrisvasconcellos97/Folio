import { C } from "../../lib/colors";

export function RouteBuilder({ accounts, userId }) {
  return (
    <div style={{ textAlign: "center", padding: "60px 20px", color: C.textMuted, fontSize: 13 }}>
      Route Builder — coming soon.
    </div>
  );
}
